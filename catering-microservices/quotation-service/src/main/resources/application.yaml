server:
  port: 8084

spring:
  application:
    name: QUOTATION-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/quotation_db
    username: root
    password: Prakash@3839

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka